A package that stubs out some MUI props/components, and can be used as a context root for tests that require a resolved analysis context with access to them.

To use, see `SharedAnalysisContext.mui_stub`.
